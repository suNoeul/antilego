# AGENTS.md — Antilego 작업 규칙

이 파일은 Claude, Codex 등 이 저장소에서 일하는 모든 에이전트가 읽는 단일 규칙 문서다.
`CLAUDE.md`는 이 파일을 가리키기만 한다. 규칙을 바꿀 때는 여기만 고친다.

## 프로젝트 한 줄

성경을 읽다가 낯선 지명에서 멈추지 않게 하는 읽기 도구. **본문이 주인공, 지도는 각주.**
지명을 탭하면 읽던 자리에서 양식화된 미니맵이 펼쳐진다.

궤적: 데이터 → 프로토타입(한 장) → UI 없는 코어 라이브러리+CLI → 웹 → 에이전트 플러그인(MCP) → 모바일 앱.
현재 단계: **02-c 패널 리사이즈 배포(2026-09-21) — 큰 화면에서 패널 가장자리를 끌어 지도를 넓힌다. 피드백 대기.**
라이브: https://sunoeul.github.io/antilego/  결과: `spikes/02-map-panel/RESULT.md` (02 · 02-b · 02-c), `spikes/03-eras/RESULT.md`, `spikes/01-web-prototype/RESULT.md`

## 두 공간

| 공간 | 위치 | 역할 |
|---|---|---|
| 온라인 | 노션 `🐳 Antilego Project` (아래 링크) | **결정 · 조사 · 설계의 정본.** 읽고 합의하는 곳 |
| 로컬 | `/Users/snow/Workspace/Antilego` (이 저장소) | **코드 · 데이터 · 실험.** 만드는 곳 |

- 노션: https://app.notion.com/p/3ded1323042d803784f6c970185d702b
  - `📚 Resources` DB — 데이터/API/지도 소스, 비용·라이선스·판정
  - `🧭 Decisions` DB — 설계·기술 결정 기록(ADR). Spike 판정이 여기로 올라간다
  - 설계 문서 4장 — 데이터 모델 / 지도 레이어 & 렌더링 / UI 네비게이션 / 아키텍처 & 확장 로드맵
- 같은 내용을 두 곳에 쓰지 않는다. 로컬 `docs/`는 노션 접근이 없는 에이전트를 위한 **스냅샷**이며,
  설계가 노션에서 바뀌면 노션이 맞다.
- Spike가 끝나면 `spikes/NN-이름/RESULT.md`에 판정과 근거를 쓰고, 노션 접근이 있는 에이전트가
  `Decisions`에 한 줄로 올린다. 노션 접근이 없는 에이전트는 RESULT.md만 쓰고 "노션 반영 필요"라고 남긴다.

## 역할 분담 (초안 — 운영하면서 조정)

| 영역 | 담당 | 이유 |
|---|---|---|
| 조사 · 라이선스 검토 · 설계 문서 · 노션 동기화 | **Claude** | Notion MCP 보유, 리서치·디자인 판단 |
| UI/디자인 판단 (양식화 맵 스타일, 읽기 뷰) | **Claude** | |
| 데이터 파이프라인 · 변환 스크립트 · 테스트 · 반복 실행 | **Codex** | 실행-수정 루프가 많은 작업 |
| Spike 코드 (`spikes/`) | **Codex** 기본, Claude는 설계 리뷰 | |
| 최종 판단 · 충돌 관리 · 방향 결정 | **Snow** | |

경계는 파일이 아니라 **디렉토리**다: `docs/` · 노션 ↔ `spikes/` · `data/`.
자기 영역 밖을 고쳐야 하면 고치되, 커밋 메시지나 RESULT.md에 이유를 남긴다.

### 현재 작업 중 (충돌 방지용 — 시작할 때 적고 끝나면 지운다)

| 누가 | 어디 | 무엇 | 시작 |
|---|---|---|---|
| — | — | — | — |

## 디렉토리

```
docs/          컨셉·데이터 조사·계획 스냅샷 (정본은 노션)
data/raw/      원본 다운로드. git에 넣지 않는다. 재현 스크립트만 커밋
data/derived/  가공 결과 (Place / Mention / Layer …). 작은 것만 커밋
spikes/NN-이름/  실험 하나 = 디렉토리 하나. RESULT.md 필수
```

## 원칙 — 모호하면 보여주지 않는다

이 도구는 성경을 읽으면서 **기독교적·개신교적 이해를 돕는 보조 수단**이다. 신학적으로 모순되거나 잘못된 정보를 제공하면 안 된다.
그래서 지리·시대 정보가 **모호한 곳은 그리지 않는다** — 없는 확신을 만드는 것보다 빈칸이 낫다. (2026-09-18, Snow)
- 위치가 확정되지 않은 지명(에덴, 아라랏 등)에 영역을 그리지 않는다. 창 1–11은 시대로 보여주지 않는다.
- 시대를 못 박기 어려운 책(시편·욥·족보)은 캡션을 띄우지 않는다.
- 연대 논쟁이 있으면 한쪽을 고르지 않고 범위로 쓴다 ("BC 15세기 또는 13세기").
- 나라 영역은 실선 국경이 아니라 점선 테두리 + 옅은 색 + "대략" 표시. 겹침은 겹친 채로 둔다.

## 하지 말 것

- **비상업 전용.** 광고는 기각(2026-09-18, Snow). 수익 구조를 붙이지 않는다. 후원 여부는 별도 결정 전까지 보류.

- **개역개정(1998) 본문을 저장소에 넣지 않는다.** 저작권 유효. v1 본문은 개역한글(1961)만.
- 개역한글 본문을 **수정·현대어화하지 않는다.** 동일성유지권. 표시할 때 그대로.
- 출처 표기 없는 데이터 사용 금지. 모든 소스는 `Resources` DB에 라이선스가 적혀 있어야 한다.
- ODbL(OSM) 데이터를 파생 데이터에 섞기 전에 Decisions에 먼저 올린다 (공개 의무 발생).
- `data/raw/` 커밋 금지.
- 실패한 spike를 지우지 않는다. 안 되는 이유가 제일 값진 기록이다.

## 커밋

- 한국어 메시지, 첫 줄은 무엇을 왜 했는지 한 문장.
- 에이전트가 만든 커밋은 본문 끝에 `Co-Authored-By:` 로 어느 에이전트인지 남긴다.
- 커밋·푸시는 Snow가 요청할 때만.

## 핵심 데이터 소스 (요약 — 상세는 노션 Resources)

| 용도 | 소스 | 라이선스 |
|---|---|---|
| 지명 → 좌표 + 언급 구절 | OpenBible.info Bible Geocoding | CC BY 4.0 |
| 고유명사 개별 ID (동명이지 구분) | STEPBible TIPNR | CC BY 4.0 |
| 한글 본문 | 성경전서 개역한글판 (대한성서공회, 1961) | 저작재산권 만료. 성명표시·동일성유지 준수 |
| 영문 → 한글 지명 매핑 | **없음 — 우리가 만든다** (Spike 00) | 완성 후 CC BY 공개 예정 |
