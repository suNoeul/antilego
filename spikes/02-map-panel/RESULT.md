# Spike 02 — 지도 패널 하나 · 결과

2026-09-18 · Fable (Claude Code). 스펙: [`docs/03-prototype-spec.md`](../../docs/03-prototype-spec.md)
"UI 스펙 (B) — Spike 02 개정". 돌리는 법은 [README.md](README.md).

---

## 무엇을 바꿨나

### 1. 밑줄 제거

`.place` 에서 `border-bottom: 1px dotted` 와 `:hover { border-bottom-style: solid }` 를 걷어냈다.
남은 것은 **색(`--accent`)** 하나뿐이다. hover 는 아주 옅은 배경(`--hl-soft`, alpha .07),
선택은 기존대로 배경 하이라이트(`--hl`, alpha .13). `text-decoration` 은 명시적으로 `none`.

### 2. 지도가 하나로

없앤 것 — `aside.side` + `#side-card`(스티키 사이드 카드), `.inline-card`(모바일 인라인 카드),
`<details id="chapmap">`("▸ 이 장의 지도"), `#modal` 전체("크게 보기"). `app.js` 의 `makeCard`,
`openModal`, `closeModal`, `wide()`, `state.selV` 도 같이 사라졌다.

남은 것 — `#panel` 하나. 폭에 따라 성격만 바뀐다.

| 폭 | 모양 | 본문 |
|---|---|---|
| ≥ 1200px | 오른쪽 서랍 400px | `body { margin-right: 400px }` 로 **밀려난다** |
| 900–1199px | 같은 서랍이 **덮는다** | 뒤에 가림막 `#scrim` (누르면 닫힘) |
| < 900px | 아래에서 올라오는 시트 55vh, 위에 드래그 손잡이 | 그대로. 시트를 내리면 다시 읽힌다 |

여는 길은 둘이다. **플로팅 버튼**(오른쪽 가장자리 세로 중앙 / 모바일은 오른쪽 아래, 글자 `지도`↔`닫기`,
`aria-expanded`)과 **본문의 지명 클릭**. 지명을 누르면 패널이 없던 상태에서도 열리면서 그 지명이 강조된다.
패널이 열리면 버튼은 패널 가장자리로 옮겨 붙는다(`right: 400px` / 시트 위 12px).

패널 안은 위에서 아래로: 지도 SVG(4:3, 최소 240px) → 확대 버튼 `+` `−` `⟲`(지도 오른쪽 아래) →
지명 블록(한글 18px 굵게 / 영문 12px 회색 / "이 장에서 N회 · 성경 전체 M회") →
**빈 `#era-caption`**(Spike 03 자리, 비어 있으면 `display:none`) → 출처 한 줄.

상태는 `localStorage['panel']` 에 남는다(try/catch). `Esc` 로 닫힌다. 장을 옮겨도 상태는 유지되고
새 장의 Scene 으로 다시 그린다.

### 3. 확대·축소

`renderScene(svgEl, scene, layers, tokens, **view**)` — 다섯 번째 인자가 늘었다.
`view = { z, px, py }`, 기본 `{1, 0, 0}` = Spike 01 과 같은 그림. `clampView`·`zoomAt`·`BASE_VIEW`·
`W`·`H`·`ZOOM_MIN`·`ZOOM_MAX` 를 같이 export 한다. 함수는 여전히 순수하다(fetch·전역 상태 없음).

**`<g transform>` 을 쓰지 않았다.** 확대할 때마다 좌표를 **다시 계산**해서 그린다. 그래서

- 점 반지름(3 / focus 5)과 글자 크기(12 / focus 14)가 viewBox 단위로 고정 → 화면에서 항상 같은 크기
- 라벨 겹침 판정이 **배율마다 화면 공간에서 다시** 돈다 → 1× 에서 숨었던 라벨이 확대하면 되살아난다 (LOD)
- 화면 밖(여유 8px)으로 나간 점·라벨은 아예 그리지 않는다

조작: 휠(커서 기준) · 드래그(포인터 1개) · 핀치(포인터 2개, 중점 기준) · 더블클릭(1.8배) ·
`+` `−`(1.6배씩) · `⟲`(scene bbox 로 복귀). 배율은 1×–8× 로 자르고, 이동은 내용이 화면 밖으로
완전히 빠지지 않게 자른다. 장이 바뀌거나 선택이 바뀌면 자동으로 초기화된다.

`+` `−` 는 **화면 한가운데가 아니라 지명이 모인 자리**(선택된 지명, 없으면 이 장 지명들의 무게중심)를
기준으로 확대한다. 이유는 아래 "판단" 1번.

---

## 검증 (헤드리스 Chrome, CDP)

`cd web && python3 -m http.server 8021` + `Google Chrome --headless=new --remote-debugging-port=9341`
(Chrome 153.0.8010.52), Node 25.8.1 내장 WebSocket 으로 CDP 직접 호출.
`Emulation.setDeviceMetricsOverride` 로 1400×900 · 1000×800 · 360×800(`mobile:true`),
`setEmulatedMedia` 로 `prefers-color-scheme: light` 고정. 매 장마다 `localStorage.clear()` 후 재적재.

### 밑줄이 정말 없는가 — 계산 스타일

세 장 전부 `document.querySelector('.place')` 의 `getComputedStyle`:

| | `#Josh.10` | `#Acts.27` | `#Gen.12` |
|---|---|---|---|
| `text-decoration-line` | **none** | none | none |
| `border-bottom-width` / `-style` | **0px / none** | 0px / none | 0px / none |
| `border-top-width` | 0px | 0px | 0px |
| `outline-style` / `box-shadow` | none / none | none / none | none / none |
| `color` | `rgb(138, 90, 43)` = `--accent` | 〃 | 〃 |

다크 모드에서도 `text-decoration-line: none`, `color: rgb(217, 160, 102)`.

### 옛 UI 가 정말 사라졌는가 (세 장 전부)

| 선택자 | 개수 |
|---|---:|
| `.side, #side-card` | **0** |
| `.inline-card` | **0** |
| `#chapmap, .fold, details` | **0** |
| `#modal, .modal` | **0** |
| `#era-caption` | **1** (비어 있음) |

장별 `button.place` 수는 Spike 01 F 이후 데이터 그대로다: 여호수아 10장 **70**,
사도행전 27장 **25**, 창세기 12장 **14**.

### 패널 열림/닫힘

| 단언 | 관측값 | 판정 |
|---|---|:--:|
| `localStorage` 비운 첫 방문에 닫힘 (세 장) | `panel-open` 없음, `aria-expanded=false`, 버튼 글자 `지도`, `visibility: hidden` | ✅ |
| `기브온` 클릭 → 패널 열림 + 해시 | `#Josh.10/aede336`, `panel-open` 있음 | ✅ |
| 지명 블록 | `기브온` / `Gibeon` / `이 장에서 9회 · 성경 전체 41회` | ✅ |
| 같은 지명 9개 전부 눌린 표시 | `.place[aria-pressed="true"]` **9** | ✅ |
| focus 점이 더 크다 | `r=5` **1개**, `r=3` **19개** (최대 5) | ✅ |
| focus 라벨이 굵고 크다 | `기브온` `font-size=14` `font-weight=700`, 나머지 12/400 | ✅ |
| 같은 지명 재클릭 → 해제, 패널 유지 | 해시 `#Josh.10`, `pressed` **0**, 패널 열린 채, 문구 "지명을 누르면 위치를 보여줍니다" | ✅ |
| 플로팅 버튼 토글 | 열림→닫힘(`지도`/`false`) → 다시 열림(`닫기`) | ✅ |
| `Esc` | 닫힘, `localStorage['panel'] = "0"` | ✅ |
| 열어 두고 새로고침 | 열린 채로 복원, `localStorage['panel'] = "1"` | ✅ |
| 장 이동 후 패널 유지 + 장면 재렌더 | `#Josh.10` → `#Gen.12`, 열린 채, `aria-label` = "지도: 하란, 가나안, 세겜, 모레, 벧엘, 아이, 남방, 애굽", 점 **8개** | ✅ |
| 모바일 지명 탭 → 패널 열림 | `#Josh.10/aede336`, `기브온` | ✅ |

### 확대·축소 (여호수아 10장, 장소 20곳)

`+` 버튼과 같은 경로(`zoomAt(view, ...anchor(), f)`)로 배율을 정확히 맞춰 잰 값:

| 배율 | `text` 개수 | `circle` 개수 | `font-size` | `r` |
|---:|---:|---:|---|---|
| **1×** | **8** | 20 | 12 | 3 |
| **3×** | **10** | 12 | 12 | 3 |
| 8× (상한) | 6 | 6 | 12 | 3 |

- **1× → 3× 에서 라벨이 8 → 10 으로 늘었다.** 1× 에서는 20곳 중 8곳만 라벨을 얻지만
  3× 에서는 화면에 남은 12곳 중 10곳이 라벨을 얻는다. 확대하면 겹침이 풀린다는 뜻이다.
- **글자 크기와 점 반지름은 모든 배율에서 12 / 3 으로 같다.** 화면에서 커지지 않는다.
- 배율 상한이 실제로 걸린다: 8× 에서 다시 5배를 요청해도 `z = 8` 그대로.

조작별 실측:

| 조작 | 관측값 | 판정 |
|---|---|:--:|
| 휠 (`deltaY = -240`, 지도 중앙) | `z` 1 → **1.696** | ✅ |
| 드래그 (왼쪽 60px · 위 30px) | `px` −111 → **−163**, `py` −83.2 → **−109.3** | ✅ |
| `⟲` | `{ z: 1, px: 0, py: 0 }` | ✅ |
| `−` 비활성 조건 | `z ≤ 1` 에서 `[disabled]`, `z ≥ 8` 에서 `+` 가 `[disabled]` | ✅ |

렌더 시간(여호수아 10장, Natural Earth 3레이어, 20회 평균): **1× 3.13ms · 3× 2.00ms · 8× 2.10ms**.
확대·드래그마다 통째로 다시 그리는 설계인데도 목표 50ms 에 한참 못 미친다.

### 레이아웃

| 폭 | 상태 | 본문 폭 | 패널 왼쪽 | 겹침 | `body` margin-right | `scrollWidth` |
|---|---|---:|---:|---:|---:|---:|
| 1400 | 닫힘 | **640** | — | — | 0px | — |
| 1400 | 열림 | **640** | 985 | **0** | **400px** | 1385 (< 1400) |
| 1000 | 열림 | 640 | 585 | 덮음(의도) | 0px | 985 |

1400 열림에서 본문 오른쪽 끝 813 < 패널 왼쪽 985 → **본문이 한 픽셀도 가려지지 않는다.**
본문 폭 640px ≥ 600px 요구를 만족한다.
1000(태블릿)에서는 가림막이 `visibility: visible`, `opacity: 1` 로 떠 있고 본문은 밀리지 않는다 — 스펙대로.

### 모바일 360×800

| 장 | 닫힘 `documentElement.scrollWidth` | 열림 `scrollWidth` | 시트 top / 높이 | 상단바 가림 | 플로팅 버튼 |
|---|---:|---:|---|:--:|---|
| `#Josh.10` | **360** | **360** | 360 / 440 (=55vh) | **아니오** (상단바 bottom 48) | 시트 위 (bottom 452) |
| `#Acts.27` | **360** | **360** | 360 / 440 | 아니오 | 시트 위 |
| `#Gen.12` | **360** | **360** | 360 / 440 | 아니오 | 시트 위 |

`body.scrollWidth` 도 전부 360. **가로 스크롤 없음.**

닫힌 시트 뒤에서 지명이 눌리는지 두 가지로 확인했다:

- `elementFromPoint(기브온 중심)` → `.place` (시트가 아니다). 패널 `visibility: hidden`.
- 실제 마우스 클릭(`Input.dispatchMouseEvent`) → 선택 토글되고 패널이 열렸다.

### console.error

데스크톱 · 태블릿 · 모바일 세 세션 전부 **`console.error` 0건, uncaught exception 0건.**
(다크 모드 + 두 낱말 지명 `아드리아 바다` 클릭 세션도 0건.)

### 스크린샷 (`shots/`)

| 파일 | 무엇 |
|---|---|
| `desktop-closed.png` | 1400×900, 여호수아 10장, 패널 닫힘 — 오른쪽 가장자리에 `지도` 알약만 |
| `desktop-open.png` | 같은 화면, `기브온` 선택 — 본문이 왼쪽으로 밀리고 패널 400px |
| `desktop-zoom3-josh10.png` | 같은 화면 3× — 1× 에서 없던 립나·라기스·막게다·에글론·드빌 라벨이 나타난다 |
| `desktop-open-dark.png` | 다크 모드, 사도행전 27장, `아드리아 바다` 선택 |
| `tablet-open.png` | 1000×800 — 서랍이 덮고 뒤에 가림막 |
| `mobile-closed.png` / `mobile-open.png` | 360×800 — 시트 닫힘 / 열림 |
| `live-josh10.png` | **배포된** https://sunoeul.github.io/antilego/#Josh.10 |

---

## 판단 (스펙에서 정하거나 벗어난 것)

1. **`+` `−` 의 기준점을 화면 한가운데가 아니라 "지명이 모인 자리"로 했다.**
   한가운데 기준으로 처음 만들었더니 여호수아 10장에서 라벨이 **1× 8개 → 2.56× 7개 → 4.1× 4개**로
   *줄었다*. 가데스바네아가 남쪽으로 끌어당긴 bbox 의 한가운데가 지명이 없는 자리여서, 확대할수록
   지명들이 화면 밖으로 밀려난 것이다. 기준을 선택된 지명(없으면 이 장 지명들의 무게중심)으로 바꾸니
   **1× 8 → 3× 10 → 8× 6** 이 됐다. 확대의 목적이 "작은 지명을 더 보는 것"이므로 이쪽이 맞다.
   휠·핀치·더블클릭은 스펙대로 커서 기준 그대로다.
2. **라벨을 viewBox 밖으로 나가게 두지 않고, 화면 밖 점·라벨은 아예 그리지 않는다.**
   Spike 01 C 의 단언 17("라벨이 viewBox 밖으로 나감 0건")을 확대 상태에서도 지키기 위해서다.
   대신 이 컬링 때문에 아주 크게 확대하면 라벨 수가 다시 줄어든다(8× 에서 6개). 정상이다 —
   화면에 지명이 6곳밖에 안 남았기 때문이다.
3. **`<g transform>` 대신 매 프레임 재계산.** 스펙이 둘 다 허용했다. 재계산 쪽이 라벨 배치와
   점 크기를 화면 공간에서 그대로 다룰 수 있어 LOD 규칙이 한 군데로 모인다. 비용은 2~3ms 다.
4. **데스크톱에서 본문을 미는 방법은 `body { margin-right }`.** `.cols` 에 `padding-right` 를 주면
   `max-width: 1040px; margin: 0 auto` 와 겹쳐 본문이 패널 밑으로 들어간다. `body` 마진은 상단바까지
   같이 밀어 줘서 화면이 한 덩어리로 좁아진다. `position: fixed` 인 패널은 영향받지 않는다.
5. **모바일 시트 손잡이는 아래로 50px 이상 끌면 닫힌다.** 스펙은 "드래그 손잡이"만 요구했다.
   끄는 동안 시트가 따라 내려가는 애니메이션은 넣지 않았다(놓을 때 판정만).
6. **`window.__antilego = { state, drawMap, setPanel, anchor }`.** 배율을 정확히 3× 로 맞춰
   라벨을 세려면 앱 내부 상태에 닿아야 했다. 앱 동작에는 관여하지 않고, Spike 03 검증에도 쓸 수 있다.
7. **아이콘.** 스펙이 허용한 `+` `−` `⟲` 를 확대 버튼에 썼다. 나머지는 그대로 `‹ › ☾` 이고,
   `▸` 는 접힌 지도와 함께 사라졌다. 플로팅 버튼과 닫기는 글자다.
8. **`renderScene` 반환값이 `svgEl` 에서 `{ view, labels, shown, project }` 로 바뀌었다.**
   `project(lon, lat)` 는 현재 view 기준 viewBox 좌표를 준다(위 1번에 필요). 반환값을 쓰던 곳은 없었다.

---

## 남은 것 / 알려진 결함

- **패널 아래쪽이 비어 있다.** 데스크톱 400×(화면 높이) 서랍에 지도 4:3(약 370×278)과 지명 세 줄뿐이라
  아래 절반이 빈다. `#era-caption` 이 채울 자리이고(Spike 03), 그 전까지는 허전하다.
- **태블릿(900–1199)에서 패널이 본문을 덮는다.** 스펙이 지정한 동작이지만, 1000px 에서 본문 오른쪽
  약 235px 이 가려진다. 1200 문턱을 낮출지는 Snow 판단.
- **8× 상한이 레반트 안쪽 장에서는 넉넉하지만 사도행전 27장처럼 넓은 장에서는 부족할 수 있다.**
  bbox 가 지중해 전체라 8× 여도 도시 하나 규모로는 못 들어간다. 필요하면 상한을 올리면 되지만
  Natural Earth 1:10m 단순화(0.002°) 한계가 먼저 보일 것이다.
- **핀치 확대는 실제 기기에서 확인하지 못했다.** 헤드리스에서 포인터 두 개를 흉내 내는 경로는
  코드상 같지만(`pointerdown` × 2), 이번 검증표에 넣을 수 있는 실측값이 없다.
- **모바일에서 플로팅 버튼이 본문 위에 뜬다.** 시트가 닫혀 있을 때는 오른쪽 아래, 열려 있을 때는
  시트 위 12px. 읽는 동안 본문 한 줄의 오른쪽 끝을 가릴 수 있다.
- Spike 01 에서 넘어온 것들은 그대로다 — `‹ ›` 가 권 경계를 넘어가고, 오프라인이면 Google Fonts 가
  죽고 시스템 serif 로 떨어진다.
- 데이터 쪽 미해결 플래그(Spike 01 D 의 "Snow 확인 필요" 1~7번)는 이번에 손대지 않았다.

---

## 배포

`web/**` 변경이라 `.github/workflows/pages.yml` 이 자동으로 돈다.

- Pages 워크플로: **success**
- `curl -sI https://sunoeul.github.io/antilego/` → **HTTP/2 200**
- 배포본 `#Josh.10` 를 헤드리스 Chrome 으로 다시 열어 확인:
  `.place` **70개**, `text-decoration-line: none`, 패널 기본 닫힘, `기브온` 클릭 → 패널 열림 +
  해시 `#Josh.10/aede336`, `console.error` **0건**. 스크린샷 `shots/live-josh10.png`.

## 노션 반영 필요

`Decisions` 한 줄: **지도는 패널 하나 · 지명 밑줄 없음 · 지도 확대 1×–8× (Spike 02, 2026-09-18)**.

---

# 02-b · 다듬기

2026-09-18 · Fable (Claude Code). Snow 피드백 네 가지 — **이름 없는 점 숨김 · 점·글자 축소 ·
지도가 움직이지 않는다(이동/가장자리/잘린 라벨) · 캐시 버스팅**. 코드만 바꿨다(`web/`, 워크플로).
`web/data/` 와 `data/`, `spikes/03-eras/` 는 건드리지 않았다.

## 1. 이름 없는 점은 그리지 않는다

`map.js` 가 점과 라벨을 **함께** 결정한다. non-focus 지명은 라벨 자리를 못 찾으면 점도 그리지 않는다.
focus 는 언제나 그린다. 여호수아 10장(장소 20곳), 데스크톱 1400×900:

| 배율 | 옛 판 `circle`/`text` | 새 판 non-focus `circle`/`text` | focus |
|---:|---:|---:|---:|
| 1× (선택 없음) | 20 / 6 | **13 / 13** | 0 / 0 |
| 3× | — | **11 / 11** | 0 / 0 |
| 8× | — | **5 / 5** | 0 / 0 |
| 1× (`기브온` 선택) | 20 / 6 | **11 / 11** | 1 / 1 |
| 3× (〃) | — | **16 / 16** | 1 / 1 |
| 8× (〃) | — | **2 / 2** | 1 / 1 |

여호수아 11장(25곳)에서는 옛 판이 `circle` **25** / `text` **7** — 이름 없는 점이 18개였다.
새 판은 **13 / 13**. 어느 배율에서도 non-focus 점 수 == 라벨 수다.
1× 라벨이 6~8개에서 11~13개로 오히려 늘었는데, 글자가 작아지고(2번) 라벨 자리가 네 방향으로
늘어난(3번) 덕이다.

## 2. 점·글자 축소

| | 옛 | 새 | 실측(`getComputedStyle`) |
|---|---:|---:|---|
| non-focus 점 `r` | 3 | **2** | `2px` |
| focus 점 `r` | 5 | **4** | `4px` |
| non-focus 라벨 | 12px / 400 | **11px / 400** | `11px` |
| focus 라벨 | 14px / 700 | **13px / 700** | `13px` |

viewBox 단위 고정이라 화면 크기는 배율과 무관하게 같다(1×·3×·8× 모두 같은 값).

## 3. 지도가 움직인다

### (a) 이동 범위 — scene bbox 가 아니라 지형 전체

`clampView(view, bounds)` / `zoomAt(view, cx, cy, f, bounds)` 로 바뀌었다. `bounds` 는
`renderScene` 이 `geo/meta.json` 의 bbox `[8, 24, 50, 43]` 를 현재 투영으로 옮겨 계산해
반환값에 실어 준다(`layers.bbox` 없으면 `map.js` 의 `GEO_BBOX`). scene bbox 는 이제
**첫 그림(⟲ 복귀)만** 정한다.

여호수아 10장, 1×, 지도 SVG 369×276.8px (viewBox 320×240 → 1px = 0.867 단위):

| 조작 | 결과 `view` | 판정 |
|---|---|:--:|
| 기준 | `{ z:1, px:0, py:0 }` | |
| 왼쪽 200px 드래그 | `px = −173.4` | ✅ (옛 판은 1× 에서 **0 고정**) |
| 오른쪽 200px | `px = +173.4` | ✅ |
| 위 200px | `py = −173.4` | ✅ |
| 아래 200px | `py = +173.4` | ✅ |
| 왼쪽·위 2000px (끝까지) | `px = −1703.1`, `py = −978.5` 에서 멈춤 | ✅ |

멈춘 자리의 `bounds` 는 `x [−2744, 1783]`, `y [−1358, 1039]` 라서 화면에 남는 지형은
가로 `80 / 320 = 25%`, 세로 `60.5 / 240 = 25%` — 설계한 대로 정확히 4분의 1이다.
그 상태에서도 land path 는 그려져 있고(지명 점은 0개, 그 자리에 지명이 없으니 당연하다).

### (b) 가장자리 fade

`.map-wrap::after` 에 네 변 24px 그라디언트(`--panel-bg` → `--panel-bg-0`, alpha 0 인 같은 색).
`pointer-events: none` 이라 드래그는 그대로 통과한다. 확대 버튼은 `z-index: 2` 로 fade 위에 둔다.

| 테마 | `--panel-bg` | 실제 그라디언트(계산값) |
|---|---|---|
| light | `#fffdf8` | `linear-gradient(to right, rgb(255,253,248), rgba(255,253,248,0) 24px)` ×4변 |
| dark | `#201d1a` | `linear-gradient(to right, rgb(32,29,26), rgba(32,29,26,0) 24px)` ×4변 |

스크린샷: `shots/b-josh11-araba-desktop.png`(light) · `shots/b-edge-fade-dark.png`(dark).

### (c) 잘린 라벨 없음 + focus 는 언제나 보인다

라벨 자리는 **아래 → 위 → 오른쪽 → 왼쪽** 순으로 찾고, 네 자리 모두 화면 안(여백 8px)에
통째로 들어가지 못하면 그 지명은 (1번 규칙에 따라 점까지) 그리지 않는다.
focus 는 예외 — `app.js` 의 `fitFocus()` 가 **배율을 건드리지 않고** 점+라벨이 여백 24px
안에 들어오도록 최소한으로 이동한다(라벨 자리가 바뀔 수 있어 최대 3회 반복).

**전수 검사** — 여호수아 11장(25) · 여호수아 10장(20) · 사도행전 27장(20) · 창세기 12장(8),
**73곳을 하나씩 선택**해 focus 상자가 24px 여백 안에 있는지, 실제 `text.getBBox()` 가 8px 여백
안에 있는지 확인: **어긋난 곳 0.**

자동 이동이 실제로 걸린 곳은 73곳 중 2곳이었다.

| 장 / 지명 | 이동 전 focus 상자 | `view` | 이동 후 라벨 `getBBox` |
|---|---|---|---|
| 여호수아 11장 `세일` | `[154.5, 196.0, 178.4, 220.0]` (아래 216 초과) | `py 0 → −4` | `[155.2, 203.9, 177.6, 218.6]` |
| 여호수아 10장 `가데스` | `[86.1, 196.0, 122.0, 220.0]` | `py 0 → −4` | `[87.2, 203.9, 121.0, 218.6]` |

`#Josh.11/a2bb5ef`(아라바) 자체는 **이동이 필요 없었다.**

| | 옛 판(커밋된 Spike 02) | 새 판 |
|---|---|---|
| 아라바 라벨 `getBBox` | `[141.6, 194.1, 178.0, 210.6]` | `[142.9, 196.9, 176.7, 211.6]` |
| focus 점 | `(159.8, 189)` r=5 | `(159.8, 189)` r=4 |
| `view` | `{1, 0, 0}` | `{1, 0, 0}` (이동 0) |
| `circle`/`text` | 25 / 7 | 13 / 13 |

**벗어난 점**: 이 URL 에서 "라벨이 위쪽 가장자리에서 잘린다"는 증상은 헤드리스 Chrome
1400×900 의 **커밋된 판에서 재현되지 않았다** — 옛 판에서도 아라바 라벨은 viewBox(240) 안쪽
194.1–210.6 에 있었다. 같은 URL 에서 재현된 것은 나머지 둘이다: **1× 에서 이동이 아예 막혀
있었고**(`clampView` 가 `z=1` 이면 `px=py=0` 으로 고정), **점 25개 중 18개가 이름이 없었다.**
세 가지 조치는 요청대로 모두 넣었고, 잘림은 전수 검사(73곳)로 막혀 있음을 확인했다.

## 4. 캐시 버스팅

저장소 소스에는 리터럴 `__V__` 가 남는다.

| 자리 | 모양 |
|---|---|
| `index.html` | `styles.css?v=__V__` · `app.js?v=__V__` |
| `app.js` | `import … from './map.js?v=__V__'` |
| `app.js` | `const V = '__V__'; window.__V = V;` → `bust()` 가 **모든 데이터 fetch** 에 `?v=` 를 붙인다 |
| `pages.yml` | 업로드 직전 `sed -i "s/__V__/${GITHUB_SHA::7}/g" web/index.html web/app.js` (CI 체크아웃 사본에서만) |

로컬 `python3 -m http.server` 는 쿼리를 무시하므로 `__V__` 그대로도 동작한다(확인함).

## 검증 환경

`cd web && python3 -m http.server 8022` + 커밋된 판 비교용 `git archive HEAD web` 을 8023 에.
`Google Chrome --headless=new --remote-debugging-port=9342` (Chrome 153.0.8010.52),
Node 25.8.1 내장 WebSocket 으로 CDP 직접 호출. 1400×900 / 360×800(`mobile:true`).

- 모바일 360: `#Josh.10` · `#Josh.11/a2bb5ef` · `#Acts.27` 모두 패널 열린 채
  `documentElement.scrollWidth = 360`, `body.scrollWidth = 360` — **가로 스크롤 없음.**
- 모든 세션 합쳐 `console.error` **0건**, uncaught exception **0건.**

## 스크린샷 (`shots/`)

| 파일 | 무엇 |
|---|---|
| `b-josh11-araba-desktop.png` | 1400×900, `#Josh.11/a2bb5ef` — 아라바 라벨이 온전하고 이름 없는 점이 없다 |
| `b-josh10-1x.png` / `b-josh10-3x.png` | 여호수아 10장 1× / 3× |
| `b-edge-fade-dark.png` | 다크 모드 가장자리 fade |
| `b-mobile-open.png` | 360×800 시트 열림 |
| `b-live-josh11.png` | **배포본** `#Josh.11/a2bb5ef` |

## 남은 것 / 알려진 결함 (02-b)

- **가장자리 24px 안에 놓인 라벨은 fade 때문에 옅어진다.** 라벨은 8px 여백만 지키면 그려지므로
  8~24px 사이에 앉은 이름은 절반쯤 흐리다(예: 여호수아 11장 `동서편`). 라벨 여백을 24px 로
  올리면 사라지는 이름이 늘어난다 — 지금은 옅게라도 보이는 쪽을 골랐다.
- **`fitFocus` 가 재는 상자는 글자 폭 추정치**(`글자수 × size × 0.92`, 실측 0.865)라 실제보다
  넉넉하고, 세로는 baseline 기준이라 실제 `getBBox` 가 아래로 약 2.6px 더 내려간다.
  그래도 8px 하드 여백 안에는 항상 들어온다(전수 73곳 확인).
- **끝까지 끌면 지명이 한 곳도 안 보일 수 있다.** 화면의 25%에 "지형"이 남는다는 규칙이지
  "지명"이 남는다는 규칙이 아니다. `⟲` 로 돌아온다.
- 1× 이동이 열리면서 **`⟲` 버튼의 쓸모가 커졌다** — 되돌릴 길이 그것뿐이다.
- Spike 02 의 남은 결함(태블릿에서 본문을 덮음, 8× 상한, 핀치 미검증, 패널 아래 여백)은 그대로다.

## 배포 (02-b)

`a8ff1a6` 푸시 → Pages 워크플로 **success** (run 35325120171). `Stamp version` 단계 로그에
`version = a8ff1a6`, `app.js?v=a8ff1a6`.

| 확인 | 결과 |
|---|---|
| `curl -s https://sunoeul.github.io/antilego/ \| grep -o 'app.js?v=[0-9a-f]*'` | `app.js?v=a8ff1a6` (`styles.css?v=a8ff1a6` 도) |
| `curl -sI .../app.js?v=a8ff1a6` · `styles.css` · `map.js` | 모두 **HTTP/2 200** |
| 배포된 `app.js` 안 | `const V = 'a8ff1a6'`, `import './map.js?v=a8ff1a6'` |

배포본 `#Josh.11/a2bb5ef` 를 헤드리스 Chrome 1400×900 으로 확인:
`아라바` 라벨 `getBBox = [142.9, 196.9, 176.7, 211.6]` — 화면(320×240) 안쪽 8px 여백 충족,
non-focus `circle` **12** == `text` **12**, focus 점 `r=4` 1개, 가장자리 fade 적용,
1× 에서 150px 드래그 → `px = −130.1` (이동됨), `console.error` **0건**.
스크린샷 `shots/b-live-josh11.png`.

---

# 02-c · 패널 리사이즈

2026-09-21 · Fable (Claude Code). 요청: **큰 화면에서 패널 왼쪽 가장자리를 끌어 지도를 넓힌다.**
설계 결정(코디네이터): **넓혀도 글자·점은 커지지 않는다** — 라벨 11/13px, 점 r 2/4 는 화면에서 그대로이고,
넓어진 화면에서 겹침 판정이 다시 돌아 **라벨이 더 나타난다**(기존 LOD 규칙이 나머지를 한다).

## 무엇을 바꿨나

### 1. `map.js` — 320×240 상수를 없앴다

`export const W = 320, H = 240` 이 사라지고 `DEF_W`/`DEF_H`(못 잴 때의 기본값)만 남았다.
`renderScene` 이 **`svgEl.clientWidth/clientHeight` 를 재서** 그 픽셀 값을 그대로
`viewBox="0 0 w h"` + `width`/`height` 속성으로 쓴다 — **SVG 를 픽셀 크기로 그린다.**
그래서 글자가 CSS 로 늘어나지 않는다. 화면 공간 계산(라벨 겹침 `inside()`, 점 반지름,
이동 한계 25%, `fitFocus` 24px 여백)은 전부 실측 크기로 돈다.

틀 계산을 두 순수 함수로 떼어 냈다 (DOM 을 만지지 않는다):

| 함수 | 하는 일 |
|---|---|
| `sceneFrame(scene, W, H)` | 어떤 지명을 그릴지 + `proj` · `x0` · `y0` · `su`(도 → 픽셀) |
| `frameBounds(frame, layers)` | 지형 bbox 기준 이동 한계. `W`/`H` 를 같이 담아 돌려준다 |

`clampView(view, bounds)` 는 `bounds.W`/`bounds.H` 로 자른다 — 화면 크기를 bounds 가 들고 다닌다.
반환값에 `W, H, x0, y0, su` 가 늘었다.

### 2. `app.js` — 패널 폭 · 지도 크기 · 중심 유지

- **지도 픽셀 크기**: 폭 = 패널 안쪽 폭, 높이 = `min(폭 × 0.75, 창 높이 × 0.7)`.
  `mapSize()` 가 인라인 `width`/`height` 를 px 로 박아 CSS 상자와 viewBox 를 1:1 로 맞춘다.
- **범위**: 최소 400, 최대 `innerWidth − 640 − 2×24 − 48`. 드래그 중에도 창 리사이즈에도 자른다.
- **중심 유지**: `keepCenter(w, h)` 가 옛 렌더의 `(W/2, H/2)` 가 가리키던 장면 좌표를 구해,
  새 크기의 `sceneFrame` 에서 같은 점이 한가운데 오도록 **`px`·`py` 만** 옮긴다. 배율 `z` 는 건드리지 않는다.
- **묶기**: 리사이즈 렌더는 `requestAnimationFrame` 하나로 묶는다(드래그 한 번에 렌더 한 번).
- **기억**: `localStorage['panelW']`(try/catch). 드래그를 놓을 때·더블클릭·키보드에서만 쓴다.
  창이 좁아지면 패널은 줄지만 저장값은 그대로 → 다시 넓어지면 원래 폭으로 돌아온다.
- `⟲` 는 폭을 건드리지 않는다. 시야(배율·이동)만 되돌린다.

### 3. `index.html` / `styles.css` — 손잡이

`#resizer` 는 패널의 자식이 아니라 형제다(패널이 `overflow-y: auto` 라 안에 두면 같이 스크롤된다).
`position: fixed; right: calc(var(--panel-w) − 3px); width: 6px; cursor: col-resize`,
안에 `::before` 로 1px 선(평소 `--line`, hover·드래그·포커스 때 `--accent`).

- `z-index: 44` — 패널(40) 위, 플로팅 `닫기`(45) 아래. **버튼이 계속 이긴다.**
- `body.resizing` 에 `user-select: none` + `cursor: col-resize`,
  그리고 `.fab { transition: none }` (안 그러면 버튼이 0.2초 뒤처져 따라온다).
- `:root { --panel-w: var(--panel-w-user) }` 를 **`@media (min-width: 1200px)` 안에만** 둔다.
  그래서 덮기(900–1199)와 모바일 시트는 400px 그대로다. 손잡이도 `body.panel-open` + ≥1200 에서만 `display: block`.
- `.panel-map` 의 `min-height: 240px` 을 뺐다(app.js 가 높이를 px 로 정한다). `aspect-ratio: 4/3` 은
  JS 가 붙기 전 한 프레임용으로 남겼다.

## 검증 (헤드리스 Chrome, CDP)

`cd web && python3 -m http.server 8031` + `Google Chrome --headless=new --remote-debugging-port=9351`
(Chrome 153.0.8010.52), Node 25.8.1 내장 WebSocket 으로 CDP 직접 호출.
`Emulation.setDeviceMetricsOverride` 로 화면 크기, `setEmulatedMedia` 로 light 고정.
드래그는 `Input.dispatchMouseEvent`(Chrome 이 Pointer Events 로 합성).

### 1920×1080, `#1Kgs.12`, 패널 열림 — 400 → 700

| | 패널 400 | 패널 700 (손잡이 300px 왼쪽으로) |
|---|---|---|
| 패널 실측 | left 1520 · width **400** | left 1220 · width **700** |
| 지도 SVG `width`/`height` 속성 | **369 × 277** | **669 × 502** |
| `viewBox` | `0 0 369 277` | `0 0 669 502` |
| CSS 상자 (`getBoundingClientRect`) | 369 × 277 | 669 × 502 |
| 라벨 `font-size` | **11** | **11** |
| 점 `r` | **2** | **2** |
| **라벨 수 (1×, 레이어 끔)** | **4** | **6** |
| 라벨 수 (1×, 레이어 켬) | 지명 4 + 영역 이름 2 | 지명 6 + 영역 이름 6 |
| 영역 blob 수 | **5** | **5** |
| 본문 컬럼 `#verses` 폭 | **640** (right 1080) | **640** (right 930) |
| `.cols` 폭 | 672 | 672 |
| 가로 스크롤 | 없음 (`scrollWidth` 1920) | 없음 |

SVG 크기 = 패널폭 − 1(패널 border-left) − 28(패널 padding) − 2(map-wrap border).
높이 = `round(669 × 0.75)` = 502 (0.7×1080 = 756 에 안 걸린다).
헤드리스에 스크롤바가 없어서 스펙 예시(652×489)보다 17px 넓다 — 계산식은 같다.

다른 장에서도 같은 방향으로 늘었다 (1× · 레이어 끔):

| 장 | 400px | 700px |
|---|---:|---:|
| `#1Kgs.12` | 4 | **6** |
| `#Josh.10` | 15 | **19** |
| `#Acts.27/aae626a` (미항 선택) | 14 | **18** |
| `#Josh.11/a2bb5ef` (아라바 선택) | 15 | **20** |

### 범위 · 기억 · 되돌리기

| 단언 | 관측값 | 판정 |
|---|---|:--:|
| 새로고침 → 폭 복원 | `localStorage['panelW'] = "700"`, 패널 **700**, SVG 669×502 | ✅ |
| 1920 에서 최대치 넘어 드래그(−900px) | **1184 에서 멈춤** (= 1920 − 640 − 48 − 48), SVG **1153 × 756** | ✅ |
| 그때 본문 | `#verses` **640** (right 688), 패널 left 736 → 사이 **48px** | ✅ |
| 그때 높이 | `0.7 × 1080 = 756` 에 막힘 (1153×0.75 = 865 대신) → 화면이 가로로 길어지며 **보이는 범위도 실제로 넓어진다** | ✅ |
| 손잡이 더블클릭 | 패널 **400**, 저장값 `"400"`, SVG 369×277 | ✅ |
| 1400×900 최대 | **664** (= 1400 − 736). 더 끌어도 664 | ✅ |
| 창 1920 → 1300 (패널 700) | 패널 **564**(= 1300 − 736)로 줄고 본문 **640** 유지. 저장값은 `"700"` 그대로 | ✅ |
| 창 1300 → 1920 복귀 | 패널 **700** 으로 복귀 | ✅ |
| 1000×800 (덮기 모드) | 패널 **400** 고정, 손잡이 `display: none`, 저장값 900 이어도 무시 | ✅ |
| 360×800 (모바일 시트) | 손잡이 `display: none`, SVG 330×248, `documentElement.scrollWidth` **360**, `body.scrollWidth` **360** | ✅ |

### 손잡이

| 단언 | 관측값 | 판정 |
|---|---|:--:|
| 6px 집는 자리 | `getBoundingClientRect` left 1217 · width **6** (패널 left 1220 기준) | ✅ |
| `cursor` | `col-resize` | ✅ |
| 선 색 (기본 / hover) | `--line` / **`rgb(138, 90, 43)` = `--accent`** | ✅ |
| 다크에서도 | 기본 `rgb(50,45,39)`(`--line`), hover `--accent` `#d9a066` | ✅ |
| `닫기` 버튼이 계속 눌린다 | 버튼 가운데·오른쪽 끝 모두 `elementFromPoint` → `btn-map`. 실제 클릭 → 패널 닫힘 | ✅ |
| 손잡이 히트 | 패널 가장자리 ±3px 안에서 `elementFromPoint` → `resizer` | ✅ |

### 지도 동작 (패널 700)

| 단언 | 관측값 | 판정 |
|---|---|:--:|
| `#Acts.27/aae626a` 미항 | focus 13px bold, 점 `r=4` 1개 · `r=2` 17개, `text` 17개 == 점 17개, 라벨 `getBBox` 여백 [340.3, 306.3, 297, 189] | ✅ |
| `#Josh.11/a2bb5ef` 아라바 | `getBBox = [317.1, 402.3, 350.9, 418.3]`, 여백 [317.1, 318.1, 402.3, **83.7**] — 8px 안쪽 | ✅ |
| 같은 URL 최대폭(1184) | 아라바 여백 [558.9, 560.3, 602.3, 137.7], non-focus `circle` 20 == `text` 20 | ✅ |
| `+` 3회 | `z = 4.096`, 라벨 19 → 9, `font-size` 11 그대로, `r` 2 그대로 | ✅ |
| 드래그 120px | `px −1071.4 → −1191.4` (이동 정확히 120) | ✅ |
| `⟲` | `{z:1, px:0, py:0}`, 라벨 19 로 복귀 | ✅ |
| 리사이즈 중 중심·배율 유지 (700 → 1100, `z=4.096`) | 화면 한가운데 장면 좌표 `(0.144164, −0.251513)` → **소수 6자리까지 동일**, `z` 그대로 | ✅ |
| 가장자리 fade | 네 변 24px 그라디언트 그대로 (`--panel-bg` → alpha 0) | ✅ |
| 시대 레이어 blob 수 | 400 / 700 / 1184 모두 **5** (`regions.blobs` 5, 데이터 그대로) | ✅ |
| 시대 영역 이름 | 2 → 6 → 6 (넓어지며 자리가 생겨 되살아난다), `label_only` 동그라미 0 → 1 → 1 | ✅ |
| `대략` 배지 · 조작 버튼 자리 | 배지 = 지도 왼쪽 위 +6px, `.zoom-ctl` 오른쪽 = 지도 오른쪽 −6px — 모든 폭에서 동일 | ✅ |
| `#era-caption` | 모든 폭에서 같은 한 줄, 폭에 맞게 줄바꿈만 다름 | ✅ |

모든 세션 합쳐 **`console.error` 0건**, uncaught exception 0건, `Log` error 0건.

## 스크린샷 (`shots/`)

| 파일 | 무엇 |
|---|---|
| `c-1920-400.png` | 1920×1080 `#1Kgs.12`, 패널 400 — 지도 369×277, 라벨 4 |
| `c-1920-700.png` | 같은 장면, 손잡이를 300px 끌어 700 — 지도 669×502, 라벨 6. 글자 크기는 그대로다 |
| `c-1920-max.png` | 최대 1184 — 높이가 756 에 막혀 가로로 길어지고 나일 삼각주까지 들어온다. 본문 640 유지 |
| `c-handle-hover.png` | 손잡이 hover — 가장자리 1px 선이 `--accent` 로 물든다 |
| `c-live-1920-700.png` | **배포본** 1920×1080 에서 같은 드래그 |

## 배포 (02-c)

`e843184` 푸시 → Pages 워크플로 **success** (run 35523550914).
`curl -s https://sunoeul.github.io/antilego/ | grep -o 'app.js?v=[0-9a-f]*'` → `app.js?v=e843184`.
`map.js?v=e843184` HTTP/2 200, 배포된 `app.js` 안에 `setPanelW` 7회, `styles.css` 안에 `resizer` 5회.

배포본을 헤드리스 Chrome 1920×1080 · `#1Kgs.12` 로 확인 (`localStorage` 비우고 hard reload):

| | 패널 400 | 손잡이 300px 드래그 → 700 | 새로고침 |
|---|---|---|---|
| `window.__antilego.V` | `e843184` | 〃 | 〃 |
| SVG `width`×`height` | 369 × 277 | **669 × 502** | 669 × 502 |
| 라벨 수 | 4 | **6** | 6 |
| `font-size` / `r` | 11 / 2 | **11 / 2** | 11 / 2 |
| `#verses` 폭 | 640 | **640** | 640 |
| `localStorage['panelW']` | (없음) | `"700"` | `"700"` |
| 최대치 | 1184 | 1184 | 1184 |

가로 스크롤 없음(`scrollWidth` 1920), **`console.error` 0건**. 스크린샷 `shots/c-live-1920-700.png`.

## 벗어난 점 / 남은 것

- **스펙 예시 `652×489` 대신 `669×502`.** 헤드리스 Chrome 에 스크롤바가 없어서 패널 안쪽이
  17px 더 넓다. 계산식(`패널폭 − 1 − 28 − 2`, 높이 `min(폭×0.75, 0.7×창높이)`)은 스펙 그대로다.
  스크롤바가 자리를 차지하는 환경에서는 그만큼 줄어든다.
- **"더 넓은 범위"는 높이가 막힐 때만 문자 그대로다.** 패널을 넓혀도 `0.7 × 창 높이` 에 닿기 전까지는
  화면 비율이 4:3 그대로라 **보이는 지리 범위는 같고, 같은 범위가 크게 그려지며 라벨이 늘어난다.**
  높이가 막히면(1920×1080 기준 지도 폭 1008px = 패널 약 1039px 부터) 비율이 가로로 길어져 실제로 더 넓은 땅이 들어온다.
  z=1 에서 scene bbox 가 틀을 꽉 채운다는 02 규칙을 유지한 결과다 — 이 규칙을 깨면 넓은 패널에서
  지도가 구석에 작게 앉는다.
- **`.panel-map` 의 `min-height: 240px` 을 뺐다.** 폭 320px 미만의 아주 좁은 화면에서는 지도가
  240px 보다 낮아진다(360px 모바일은 248px 로 전과 같다).
- **손잡이는 밀어내기 모드 전용이다.** 덮기(900–1199)에서 넓히면 본문을 더 가리기만 해서 뺐다.
- 키보드로도 조절된다(`←`/`→` 24px, `Home` → 400px). `role="separator"` + `aria-valuenow`.
  다만 **스크린 리더로 실제 읽어 보지는 않았다.**
- 창을 좁혀 패널이 줄어든 상태에서 손잡이를 놓으면 그 줄어든 값이 저장된다(드래그를 끝낼 때만 쓴다).
  창만 줄였다 늘리면 저장값은 그대로라 원래 폭으로 돌아온다.
- 패널 폭은 `localStorage` 하나라 **탭마다 다르게 두지 못한다**(열림 상태·다크 모드와 같은 방식).
